# AI 홀로 — 프로젝트 안내

## 프로젝트 개요

로드맵 관리형 독서실 × 인스타그램 교육 채널 '공부중독'이 함께 만든 공부법 멘토 AI **'AI 홀로'**의 프론트엔드 시안입니다.

- 작업 파일: `chatbot_design_v5.html` (단일 HTML, CSS·JS 인라인)
- 운영자: 공부중독 송의준 선생님 (로드맵 독서실 학습 컨설턴트)

## 작업 범위

**프론트엔드(디자인·문구)만 수정합니다.**

- 챗봇 응답 로직(`send`, `mockAnswer` 등)은 건드리지 않습니다. 현재는 미리보기용 고정 응답 상태입니다.
- 실제 AI 연동은 사이트 업로드 단계에서 별도로 진행합니다.

## 화면 구조

### 사이드바
- 상단: 로고 + '나에게 맞는 학습 상담 알아보기' 버튼
- **홈**
- **공부중독에게 질문하기** — 수학 / 국어
- **로드맵 선배들의 학습 조언 살펴보기** — rm1~rm5
  - rm1 나는 로드맵에서 이렇게 공부했어
  - rm5 공부가 안 될 땐 이렇게 해봐
  - rm2 현강, 인강 강사 추천해줄게
  - rm3 대학 입시 제도 알고 있어?
  - rm4 나만의 내신, 수능 꿀팁 알려줄게
- 기본값: **닫힌 상태**로 시작

### 홈 화면
- 히어로: 로고 2개 + "공부 AI 홀로" + "혼자 공부하는 시간, 'AI 홀로'가 함께합니다"
- FAQ 버튼 4개 (한 줄, 중앙 정렬, 줄바꿈 없음)
  1. 'AI 홀로'가 뭔가요?
  2. 일반적인 AI와 무엇이 다른가요?
  3. 'AI 홀로' 사용방법
  4. '공부중독'이 누군가요?
- '바로 시작하기' 안내 박스

### 상담 화면 (`#view-consult`)
- 탭 버튼 3개: 전반적인 학습 컨설팅 / 수학 과외 / 국어 과외
- 각 패널: 소개 + '이런 분께 추천해요'
- 하단: **학습 상담 후기**(3열 카드) + **학습 상담 미리 보기**(3열 사진 자리)
  - 후기 내용과 사진은 아직 미정, 플레이스홀더 상태
  - 영상을 넣을 경우 `<video autoplay muted loop playsinline>` 사용 (무음 필수)

## 디자인 규칙

### 절대 규칙
- **강제 줄바꿈(`<br>`) 금지.** 지시가 있을 때만 넣습니다. 문장이 어중간하게 끊기는 것을 매우 싫어합니다.
- **가로폭을 끝까지 활용합니다.** 좁고 답답한 레이아웃 금지. 컨테이너 최대 폭은 1040px.
- **볼드는 소제목에만.** 본문에는 볼드를 넣지 않습니다.
- 산문 나열보다 **구조화된 시각 요소**(카드, 번호 목록, 스펙 박스)를 선호합니다.
- 한글은 `word-break: keep-all`로 단어 중간 잘림을 막습니다.

### 색상 (`:root`)
```
--cream: #F7F5F0    --paper: #FFFFFF
--ink: #17181A      --ink-soft: #5C5E58
--green: #1E6B41    --green-deep: #15522F   --green-soft: #EDF4EF
--line: #E6E3DB     --line-dark: #CFCBC0    --tan: #B99A62
```

### 강조 표기 두 종류
- `.ef-hl` — 초록 볼드 + 초록 형광펜. 각 답변의 **제목(리드 문장)** 핵심구에 사용
- `.gpt-quote` — 진한 텍스트 + 베이지 형광펜. **본문** 중 강조할 구절에 사용

### FAQ 답변 공통 구조
모든 답변은 동일한 체계를 따릅니다.
```
<span class="tag">라벨</span>        영문 라벨, 좌측 정렬
<div class="gpt-lead">              리드 문장 + 아래 구분선
<p class="gpt-p">                   본문
```
답변 박스는 `.faq-answer` → `.faq-answer-inner`(높이 제어) → `.faq-answer-body`(디자인) 3중 구조입니다.
**`inner`에 padding을 주면 접힘 상태에서 높이가 남아 버그가 생깁니다.** 반드시 `body`에만 줍니다.

### 아코디언 동작
`display:none` 방식이 아니라 `grid-template-rows: 0fr → 1fr` 트랜지션으로 부드럽게 펼쳐집니다. `inner`에는 `min-height: 0`과 `overflow: hidden`이 필수입니다.

### 스크롤바
`#view-home`, `#view-consult`에 `scrollbar-gutter: stable`이 적용되어 있습니다. 제거하면 아코디언 열 때 화면이 좌우로 흔들립니다.

## 캐러셀 (`#whoCarousel`)

'공부중독이 누군가요' 답변의 특이사항 박스 오른쪽에 있습니다.

- 슬라이드 3장 전부 **HTML/CSS로 직접 구현**했습니다. 이미지 캡처를 넣었더니 화질이 깨져서 코드로 재현한 것이니, 이미지로 되돌리지 마세요.
  1. 축구 사진(base64) + 그라데이션 + STARTING POINT 라벨
  2. 소개 카드 (크림 배경 + 검정 하단 박스)
  3. 01·02·03 번호 카드
- 4초 자동 전환, 점 인디케이터, 호버 시 정지
- 클릭하면 확대 보기(라이트박스). 원본 픽셀 크기를 측정해 통째로 `scale`하므로 줄바꿈이 원본과 동일합니다. 이 방식을 바꾸면 확대 시 글자가 잘립니다.

## 커뮤니케이션

- 요청한 부분만 정확히 수정합니다. 임의로 내용을 추가·삭제·재구성하지 않습니다.
- 수정 요청은 제안이 아니라 **명세**로 받아들입니다.
- 수정 후에는 요청 항목을 하나씩 대조해 확인합니다.
